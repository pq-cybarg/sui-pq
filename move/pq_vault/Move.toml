[package]
name = "pq_vault"
edition = "2024.beta"
version = "0.1.0"

[dependencies]
Sui      = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "framework/mainnet" }
wots     = { local = "../wots" }
slh_dsa  = { local = "../slh_dsa" }
pq_guard = { local = "../pq_guard" }

[addresses]
pq_vault = "0x0"
